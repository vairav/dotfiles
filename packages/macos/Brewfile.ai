# Brewfile.ai - AI and LLM tools
# Usage: brew bundle --file=Brewfile.ai

# Desktop Apps
cask "chatgpt"
cask "claude"
cask "cursor"
cask "windsurf"
cask "zed"
cask "trae"
cask "msty"         # Local AI model manager (multi-provider)
cask "lm-studio"    # Local only LLM model manager
cask "repo-prompt"  # Repo level AI chat tool

# CLI Tools
brew "qwen-code"

# Block Goose
tap "block/goose"
brew "block/goose/goose"
cask "block-goose"

# MCP
brew "mcp-toolbox"
# brew "github-mcp-server"      # Install manually if needed
# brew "terraform-mcp-server"   # Install manually if needed

# Manual installs:
# claude-code: npm install -g @anthropics/claude-code
# ollama: brew install ollama
